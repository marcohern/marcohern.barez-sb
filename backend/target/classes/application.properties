server.port=8080

# SQLite DataSource
spring.datasource.url=jdbc:sqlite:./data/barez.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.username=
spring.datasource.password=

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Flyway (enabled for production databases, disabled for SQLite)
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# JWT
app.jwt.secret=bXlTdXBlclNlY3JldEtleUZvckJhcmV6QXBwMTIzNDU2Nzg5MA==
app.jwt.expiration-ms=86400000

# PDF receipts
app.pdf.storage-path=./data/receipts

# Bar name (used in PDF header)
app.bar.name=Barez

# CORS
app.cors.allowed-origins=http://localhost:5173

logging.level.com.marcohern.barez=INFO

# EZCLINICME (read-only secondary datasource)
# NOTE: credentials are inline for now per project request; move to env vars / secret manager.
ezclinic.datasource.url=jdbc:sqlserver://160.153.178.56\\SQLEXPRESS:49510;databaseName=EZCLINICME;encrypt=true;trustServerCertificate=true
ezclinic.datasource.username=sa
ezclinic.datasource.password=C0l0mb14m3x$
ezclinic.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
ezclinic.jpa.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
ezclinic.jpa.hibernate.ddl-auto=none
ezclinic.jpa.show-sql=false

